spring:
  application:
    name: ollama
  ai:
    chat:
      client:
        enable: false
    openai:
      api-key: ${OPENAI_API_KEY}
    ollama:
      base-url: http://localhost:11434
      chat:
        model: gemma3:1b
        temperature: 0.7
server:
  port: 9009
#api key for open AI
